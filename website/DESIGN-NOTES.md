# NOCTA Website — 設計引き継ぎ書（DESIGN-NOTES）

最終更新: 2026-06-15

このドキュメントは NOCTA ホームページ（`website/`）の**デザインの意図・考え方・経緯**を、後から見ても理解できるように残すための引き継ぎ書です。

- **正本（source of truth）**: 現在ライブで動いているサイト（`main` ブランチの `website/` ＝ https://kutakuta1001.github.io/NOCTA/ ）。
- **トークン仕様書**: `website/DESIGN.md`（色・タイポ・コンポーネント・ホームページ構成の機械可読スペック。`@google/design.md` 形式・lint対応）。
- **このファイル**: なぜそのデザインなのか（WHY）と意思決定の記録。

仕様（数値・トークン）は DESIGN.md を、理由・背景はこのファイルを見てください。両者が食い違ったら**ライブサイトを正**とし、DESIGN.md を合わせます（逆ではない）。

---

## 1. ブランドの核となる考え方

- **多領域クリエイティブプロジェクト**であり、音楽専門レーベルではない。4本柱 = **音楽 / ビジュアル / 言葉（ブログ）/ コード（アプリ）**。
- この4本柱は、ヒーロー下の STATS（楽曲数 / ビジュアル数 / ブログ数 / アプリ数）、About本文、価値カード「越境（Crossing）」、footerタグライン `Music × Visual × Words × Code` に一貫して現れる。
- トーンは **dark editorial serif-led**（落ち着いた・余白主体・印刷的）。声高でなく、必要な箇所だけ静かに強い。

---

## 2. パレット（確定: 2026-06-15）

| 役割 | 値 | 備考 |
|---|---|---|
| アクセント（単一） | シルバー `#B8B4AE` | ブランド唯一の差し色。CTA・section-tag・hover glow に限定使用 |
| テキスト | オフホワイト `#E8E0D0` | |
| 背景 | warm-black `#0A0906` / surface `#0F0D0A` | |
| サブテキスト | `#9A8A7A` | |

**経緯**: 2026-06-04 に gold(#C4942A) → シルバー×オフホワイトへ変更。だが DESIGN.md が gold のまま残り混乱が発生。2026-06-15 に DESIGN.md をシルバーへ再構成し、サイト側の廃止値（#39FF6A ネオングリーン / #C8A2FF ラベンダー / #C4942A gold / #05050F / Space Grotesk / Inter）を全除去して整合させた。

**やってはいけない**: 第2のアクセント色を足す／白背景・ライトモード／gold・ネオン・ラベンダーの再導入／未ロードフォント（Inter・Space Grotesk）の使用。

---

## 3. タイポグラフィの考え方

- ロードフォントは **EB Garamond（セリフ大見出し）/ Syne（見出し・ラベル）/ Bebas Neue（数字・ドラマチック大文字）/ Noto Sans JP（日本語本文）** の4種のみ。
- **「大きさで強調、太さで強調しない」**: セリフ大見出しは weight 600 が上限。強調はサイズとシルバーのコントラストで作る。
- **2つの見出しダイアレクトを使い分ける**（混在は同一セクション内では避ける）:
  - Pattern A（Bebas/EB Garamond 大文字）= ポートフォリオ系（WORKS / VISUAL）。作品を「見せる」場。
  - Pattern B（section-tag + Noto Sans JP 見出し）= About / Contact。言葉を「読ませる」場。

---

## 4. レイアウト原則（Airtable 分析から移植）

`design/DESIGN-airtable.md`（Airtable マーケサイトのトークン分析）を参照テンプレートに、**色・フォントはNOCTA値に総入れ替えしつつ「原則とスケール」だけ移植**した。NOCTAはシルバー単一アクセントなので、Airtableの「多色シグネチャカード」は**色相でなく面のトーン差・スケール**として翻案している。

実装済みの原則:

1. **96px+ のセクションリズム**（`py-32`）— 全帯で一定の呼吸。
2. **サーフェスのリズム**（2026-06-15追加）— 同じ面を連続させない。`works`/`visual` を `bg-black/30` の沈み面、`about`/`apps`/`blog` を平面にして交互の抑揚を作る。沈み面はグラスカードのコントラストも保つ。
3. **ステートメント帯（voltage moment）**（2026-06-15追加）— about と works の間に浮き面（`bg-white/[0.02]` glassy）の全幅帯。大型 EB Garamond の一文＋一語だけイタリック・シルバー。色を足さずスケールと面の浮きで「山」を作る。＝ダークモード版のシグネチャカード。1ページに1つ。
4. **featured-first グリッド**（2026-06-15追加）— Works 先頭カードを `lg:col-span-2` の横長に。均質な3列グリッドの「スペックシート感」を崩す。
5. **glass-and-noise first のelevation** — 影は最小。深さは半透明グラス＋常時のグレインノイズ＋（hover時のみの）シルバーglowで作る。glow は常時点灯しない。
6. **アクセントの希少性** — プライマリCTAは1ビューポートに1つ。シルバーは差し色として控えめに。
7. **About シグネチャビジュアル**（2026-06-15・案F）— About右の図を「中央SOULノードグラフ」から **4本柱（Music/Visual/Words/Code）が中央の動くシルバー球体に収束する図**へ刷新。中央の安っぽいテキスト（SOUL/NOCTA）は廃し、メタリックなグラデーション球体（`.nocta-orb`・`orbFloat`/`orbShine` で呼吸・浮遊）に。STATS/タグラインの4本柱と図のラベルを常に一致させる。
8. **ヒーロー背景の幾何ループ**（2026-06-15）— 旧「2026」を、同心の回転多角形・ダッシュリング・脈動ドット（シルバー線画）の **30秒シームレスループ**に置換。全要素の周期は30秒の約数（30s/15s）でぴったり最初に戻る。`prefers-reduced-motion` で停止。CSSは `.hero-geo` / `.geo-*` / `@keyframes geoSpinCW/CCW/geoBreathe/geoPulse`。

**ブランド肩書き**: 公開表記は「Music Entertainment / 音楽エンタメ」→ **「Creative / クリエイティブ」** に変更（2026-06-15・`<title>`・ヒーローeyebrow・footer）。音楽専門レーベルではないため領域中立に保つ。

**見送った案**: モノクロのプルーフ帯（ロゴストリップ相当）。リリース前で「配信中」等が事実でないため。リリース後に「配信先」帯として再検討する。

---

## 5. 2026-06-15 の変更サマリ（commit）

| 変更 | commit |
|---|---|
| DESIGN.md をシルバー基調に再構成＋サイト廃止値クリーン | `8b2711c` |
| visualセクション沈み面（サーフェスリズム） | `8568810` |
| ステートメント帯追加（"遊ぶことで人生を彩ろう"） | `ca240ae` |
| Works先頭 featured（lg:col-span-2） | `fddf98d` |
| 多領域リブランド（STATS4項目化＋About拡張＋越境カード＋タグライン） | `a0db4d2` |

---

## 6. 保守メモ

- **STATS の数値は手動更新不要**。`works/visual/blog/apps` 各データ配列の件数を JS が自動カウント（`index.html` 内「Stats bar: データ配列から件数を動的カウント」スクリプト）。
- **コンテンツデータ**は `works-data.js` / `visual-data.js`（3配列）/ `apps-data.js` / `blog-data.js`。追加は各スキル（`/hp-add-work`・`/visual-add`・`/blog-publish`）参照。
- **ステートメント帯のコピー**はブランドメッセージ＝CEO領域。現在は静的（i18n未対応）。EN切替時も日本語表示。文言確定後に i18n キー追加を検討。
- **デッドコード**: `.vocaloid-bar` / `.vocaloid-card-tag` は body 未使用の旧CSS（非表示・無害）。除去は任意。
- **未整合の独立ページ**: `brand_assets.html` / `brandkit.html` は旧パレット（gold/neon/lavender）の見本ページ。index 非リンク。機械置換でなくシルバーで**再生成が必要**な別タスク。
- **lint**: `npx @google/design.md lint DESIGN.md`。contrast/orphaned-token の warning は仕様上の既知事項（DESIGN.md「Known Gaps」参照）。
- 別ページ `the-first-flower/` は**独立した世界観（継承度 低）**。専用の引き継ぎ書 `the-first-flower/DESIGN-NOTES.md` を参照。

---

## 7. 猫モチーフとストーリー（2026-06-16 追加）

NOCTA LP には縦スクロール＝物語として、**「AI＝幾何（冷たく構造的）」と「遊び＝猫（温かく色彩）」の二項対立**が通底している。

### 二項対立のルール
- **Statement より前（Hero / About / Tools）＝幾何＝AIの領域。猫は出さない。**
  - Hero: 幾何ループ（回転多角形・リング・脈動ドット・30秒シームレス）
  - About: 4本柱オービット＋中央の動く銀球体（`.nocta-orb`）
  - Tools: **意図的に猫なし・装飾なしでクリーンに保つ**（希少性＝心地よさ／ここは"AIツール"の静かな区画）。幾何アクセント案も検討したが「入れない」で確定。
- **Statement 以降＝猫が登場し、色（遊び）を持ち込む。**

### ストーリーの流れ（ページ上→下）
1. **About** — 銀球体の周囲の4本柱（Music/Visual/Words/Code）の点と線を **赤/黄/青/紫** に色付け。これは AI フェーズの4色＝後で猫が出会って「遊び」になる色の伏線。
2. **Statement「遊ぶことで人生を彩ろう」** — 銀猫が赤い絵の具皿に前足をつける（`silver-paint.png`）。物語の起点。背後 z-0・スクロールで登場。
3. **Portfolio** — 見出し周りの空きスペースに色付き水彩足跡（**SVG**・赤黄緑青紫）が 1/3速度でゆっくり1つずつ登場（猫が歩いて残した跡）。
4. **Visual** — 銀猫が座って後ろ姿でギャラリーを眺める（`silver-sit.png`）。見出し脇に配置。
5. **Blog** — 絵の具付きで歩く銀猫（`silver-walk.png`）が見出しへ向かう。
6. **フッター直前** — 色付き足跡の水平区切り線（`paw-divider.png`）。旅の continuity。
7. **Footer** — 猫が丸くなって眠る（`silver-sleep.png`）。旅の終わり。

### なぜ銀猫か
イラストは黒猫・銀猫の両方を用意。NOCTA はダーク（`#0A0906`）なので**銀猫（ぎんねこ）を使用**（黒猫はダーク背景で消えるため／黒猫はライト系の文脈用）。

### 素材
- **切り出し済みスプライト（repo にコミット済み）**: `website/img/cat/`
  - 猫: `silver-paint / walk / sit / sleep / peek .png`
  - 色付き肉球: `paw-red / amber / green / teal / blue / purple .png`
  - 区切り線: `paw-divider.png`
- **元シート（repo外・`project_NOCTA/visual/` にローカル保管。画像はgit管理外規約のため未コミット）**: `ぎんねこ透過.png / ２ / ３ / ４`, `くろねこ透過.png`（猫＋ラベル入りの全身ストーリーボード・透過）。新ポーズが要るときは PIL で `crop` → `getbbox()` 自動トリムで切り出す（座標はラベルを避ける）。**別PCにはこの元シートは渡らない点に注意**（スプライトは渡る）。
- 「猫がレインボートレイルを歩く」全身バンドも試作したが**大きすぎて不採用**→ 細い `paw-divider.png` に置換。

### 実装の仕組み
- `.cat-scene`（IntersectionObserver が監視→ `.in-view` 付与）＋ `.cat-fig`（opacity/translate でフェード登場）。猫 `<img>` は各セクションの見出し/帯の中に `z-0`（本文は `z-10`）・`pointer-events:none`・`aria-hidden` で配置。
- 足跡は `.paw-trail`>`.paw`>`.paw-svg`（`#cat-paw` シンボル＋`#watercolor` feTurbulence フィルタの色付きSVG）。**base opacity 0.55**（JS/監視が発火しなくても見える保険）、`.in-view` で `pawPress` ポップ、`animation-delay` で歩行カデンス、速度は `--paw-dur`（Portfolio は 1.8s）。
- Observer のセレクタは `.paw-trail, .cat-scene`。`prefers-reduced-motion` で全停止。

### Do / Don't
- 猫は**間引く**（約5ビートのみ）。全セクションに入れない（Tools はクリーン維持）。
- 猫は Statement 以降のみ。Hero / About / Tools は幾何（AI）のまま。
- 肉球/4本柱の色（赤/黄/青/紫）は固定セット。About → 足跡 の色対応を崩さない。

### HiNa コレクション（2026-07-05 追加）

HP本編の銀猫ストーリー（絵の具→足跡→ギャラリー→歩く→眠る）から飛び出し、**銀猫が単独で3つの静かな世界を巡る散歩**を、それぞれ1本のWebアプリとして切り出したのが **HiNa** コレクション。

| アプリ | 世界 | フッターの猫ポーズ | 足跡の色 |
|---|---|---|---|
| **Gyu**（`website/gems/`） | 宝石36種 | `silver-sleep.png`（眠る） | 5色ランダム（虹） |
| **Ichi**（`website/flora/`） | 季節の花24種 | `silver-walk.png`（歩く） | ロゴのローズピンク単色 |
| **Hare**（`website/iro/`） | 伝統色48色 | `silver-paint.png`（絵の具） | 和紙5色（琥珀/抹茶/紺/紫/金） |

- 3アプリは共通鑑賞エンジン `website/zukan/zukan-core.js` を共有（i18n・お気に入り・ダイアログ・レール・キーボードナビ等）。UIフレーム（warm-black・粒子ノイズ・EB Garamond/Syne/Shippori Mincho）も共通。
- 足跡の色は `config.pawColors`（core側の任意設定）で各アプリが個別に指定。既定値（未指定）は本編と同じシルバー単色。
- **命名規則は NuWord 方式を継承**: 各アプリ自身のページ内では単独名（`Gyu`/`Ichi`/`Hare`）のみを表示し、「HiNa」はHP Appsカードのタイトル（`apps-data.js`）にのみ「HiNa Gyu」の形で付与する（NuWord Still の `still — NOCTA` が単独ページ内で「NuWord」を名乗らない前例に合わせる）。
- コピーの語り口: 3アプリの説明文は「銀猫が見つけた◯◯を静かに眺める図鑑」で始める共通フレーミングを採用し、シリーズとしての一体感を出す。

#### HiNa 体験原則の4カ条（2026-07-05 追加）

HiNaコレクションが提供する体験を以下の順で重ねる:
1. **ひとつずつ、静かに眺める**（1画面1点の鑑賞スクロール）
2. **今日のひとつが毎日変わる**（今日の一石/一花/一色 — 日付シードで決定的に）
3. **気に入ったものに肉球の印をつける**（お気に入り・localStorage）
4. **眺めるだけで終わらず、遊んで跡を残す**（Hareの「筆あそび」= その色のインクで書く小さな書斎）

第4原則「遊びのレイヤー」は 2026-07-05 に Hare・Gyu で実装。各アプリに1つずつ「遊びのレイヤー」を足していく構想:
- **Hare**: 選んだ色のインクで筆あそび（実装済み・`website/iro/fude.js`）
- **Gyu**: 石を光にかざす（実装済み・`website/gems/kazasu.js` + `website/gems/gem3d.js`）— Phase 1で写真＋擬似光沢、Phase 2でコード生成の本物の3D宝石メッシュ（Three.js）へ。ライトボックス内でPhoto/Gemトグル。3Dジェムは58面級ブリリアントカット等のプロシージャルジオメトリ＋MeshPhysicalMaterial（transmission/dispersion/DoubleSideの内部反射）＋宝石ショーケース専用環境マップ＋Bloomで、傾けるとファセットがきらめく。実物の屈折率・分散をデータ化。Phase 1=先頭5石+オパール。Phase 2.5（2026-07-05）で本物により接近: 理想ブリリアントカット比率（パビリオン角40.75°で全反射・クラウン角34.5°）、前面ドームへ決定論配置したシンチレーション16光源、MSAA(samples:4)+DPR上限2.5（タッチ/低コア端末は1.75に抑制）、clearcoat強化によるファセット稜線のフレネル反射（ベベル効果）、ライトボックス背景の暗いビロード風ラジアルグラデで実在感。透明石の色はカタログ写真に一致（例: ダイヤは「伝説の青」ホープダイヤの#6E93C4）。Phase 3（2026-07-05・CEO「同じ形だと偽物に見える」+ hainarashi氏のBlender宝石記事の知見）でカット形状を多様化: `brilliant`をアウトライン関数（angle→半径スケール）でパラメータ化し、検証済みトポロジー（テーブル/クラウン/パビリオン/キュレット）を保ったまま round/oval/pear（涙型）/marquise（舟型）/cushion（角丸）/trillion（三角）を生成。加えて rose（アンティークの平底ドーム）・crystal（原石ポイント）・step（エメラルド）・cabochon（オパール）で計10シルエット。石名からの決定論的シードでガードルに微小な非対称ジッターと基準回転を与え、CAD的な完全対称（=偽物っぽさ）を排除。デフォルトで軽く見下ろす基準傾き（17-18°、カボション/ローズ/原石は浅め）を付けて上面の輪郭差を一目で判別可能にした。記事由来のリアル化: 硬度依存roughness（記事「完全な透明roughness=0は作り物臭い」→ 軟らかい石ほど表面を微かに曇らせ透過に深みを出す。ダイヤ0→水晶0.03→軟石0.05）、物理ベースIOR・石別dispersionをmount()へ伝達。3Dデータは6→13石に拡張（各石が異なる形）。Phase 4（2026-07-06・CEO「本物に見せたい」）でリアルタイムPBRの天井を越えるハイブリッドを導入: **動＝PBR（傾けて遊ぶ間は現行の60fps）/ 静＝パストレーシング「現像」（手を止めると1〜3秒かけて収束）**。手が止まると `three-gpu-pathtracer@0.0.24`（`three-mesh-bvh@0.9.10`・importmap CDN）の `WebGLPathTracer` が別renderer/別canvasで多重バウンスの光輸送を累積し、Cyclesに近い**内部の多重反射・干渉模様**（記事の「手前と奥のファセットの干渉」）を持つ静止画へクロスフェード。傾け入力で即PBRに復帰。PT用の光源は emissive な MeshStandardMaterial 群（three-gpu-pathtracer は MeshBasicMaterial を光源と認識しないため）＋ GradientEquirectTexture。対応判定 `canPathtrace`（WebGL2・非reduce・非coarse・非低コア・floatBuf）で、非対応端末（モバイル等）は現行PBRを維持しデスクトップ先行。制約: パストレーサは分散（ファイア）非対応のため現像した静止画にファイアは出ない（動のPBRには出る）。設計/計画: `docs/superpowers/{specs,plans}/2026-07-06-gyu-pathtracer-hybrid*.md`
- **Ichi**: 咲かせる（実装済み・`website/flora/hana.js`）— Hare「筆あそび」の対称版。花図鑑 cover の「咲かせて遊ぶ」CTA から全画面ビューへ入り、指でなぞった軌跡に手続き的な花（桜/菊/チューリップ/小花の4形・Canvas 2D）が「つぼみ→満開」で芽吹き、庭が育つ。季節パレット4種（春淡紅/夏水青/秋琥珀/冬白銀）で配色を切替、なぞる速度で花の大きさが変わる（fude の速度依存線幅の対称）。咲いた庭は「押し花にする」で和紙地に平面化・退色・季節ラベルを添えた PNG に保存。reduced-motion で即満開・タッチ対応・花総数上限400。設計/計画: `docs/superpowers/{specs,plans}/2026-07-06-ichi-sakaseru*.md`。Phase 2a（実装済み・2026-07-07「チープさ」解消と瞑想性向上）: 花びらをグラデーション＋半透明＋花びら毎ゆらぎに（脱ベタ塗り）、季節連動の暗い庭の光だまり背景（`.hana-stage` CSSに分離しpress()は透明花のまま）、奥行き（depthで奥は小さく淡く柔らか）、低opacityグレイン、開花後の微風（常時rAF＋30fpsスロットル＋sway・reduceで停止）、遅さの報酬化（ゆっくりなぞるほど大きく豊かに・速いとまばら）、呼吸のテンポ（開花約1.5秒）、入退場の儀式（入場=中央に招待花＋フェードイン／退場=花が閉じてから暗転 farewell）。設計/計画: `docs/superpowers/{specs,plans}/2026-07-07-ichi-phase2a*.md`。Phase 2b（実装済み・2026-07-07 無常と季節）: 散る花＝満開後しばらく経つと花びらが1枚ずつ落ちて淡く散り（落下パーティクル・上限120）、花は盛りを過ぎた姿（花びら数減）で残る（庭は自動消滅させない・押し花で「移ろいを留める」）。今日と連動＝汎用「咲かせて遊ぶ」は本日の季節パレットで開き（各花入口はその花の季節）、図鑑カバーに淡い季節色。図鑑セクションの余白を微増。reduce-motionでは散り・加齢なし（静的）。設計/計画: `docs/superpowers/{specs,plans}/2026-07-07-ichi-phase2b*.md`。Phase 2c（実装済み・2026-07-07 五感と一回性・5体験）: ①なぞり音＝咲く瞬間だけ鈴/水滴の短い生成音（Web Audio・素材不要・**デフォルトオフ**・ツールバー「音を添える」トグル・花が大きいほど低い音）②風の一吹き＝40〜90秒に一度、庭全体が同方向にそよぎ加齢した花から花びらが舞う ③長押しで大輪＝動かさず600ms待つと、その場に特別大きな一輪が3.5秒かけて開く（遅さの報酬の極致）④今日の一花カードから「この花を咲かせる」導線 ⑤押し花に漢数字の日付（「春 の 押し花 · 七月七日」・各花シード時は「薔薇 の 押し花 · …」）。設計/計画: `docs/superpowers/{specs,plans}/2026-07-07-ichi-phase2c*.md`。Phase 2d（実装済み・2026-07-07 大輪・葉の装飾・風の実感）: ①長押しの大輪を約2倍に（r 40-52px→82-104px・葉2枚付きで映える）②咲いた花の約半数に葉が生える＝根元にセージ緑（#6E7A55・半透明）のしずく形の葉を1〜2枚、花の背後（先に描画）に。花の rot/sway に追従し風でも一緒に揺れ、bloom に連動して開く。press() が drawFlower を再描画するため押し花に自動で葉が写る（追加処理なし）。reduce-motion でも葉は静的に付く（葉は装飾であり動きではない）③風を実感しやすく＝頻度 40-90秒→16-34秒・そよぎ係数 0.12→0.2・舞う花びら最大 3→5枚。音ON時のみ「そよ風」音（otoPlayWind: ホワイトノイズ buffer→lowpass 520Hz→gain 0.03 を 1.8秒エンベロープ・createGarden の `onWind` コールバック経由で mount の oto に接続・ノード終了時 disconnect でリーク防止）。デフォルト無音は維持し無音でも視覚で分かる。reduce時は風・風音なし。花0輪では風・風音を出さない。設計/計画: `docs/superpowers/{specs,plans}/2026-07-07-ichi-phase2d*.md`。Phase 2e（実装済み・2026-07-08 草花の標本 — 自生と標本化）: 参考画像（押し花標本ボード "BLOOM MAISON" 系）の「花＋豊かな緑が自然な余白で標本のように組み上がる」世界を、コンセプト不変で実現。①草花の緑4種を手続き生成で追加＝草・つる（sprig・小葉が連なる曲線の茎・タイム風）／シダ（fern・中軸に上外向きの羽状小葉）／レースフラワー・種穂（umbel・放射状に細茎＋先端の微小花）／小花の群れ（floret・小さな5弁花が数輪）。庭の要素に `kind` を持たせ `drawEntity(ctx,o)` が分岐（drawFlower は 'flower' 経路として完全不変）。緑drawerは全て drawFlower と同じ自己完結型（save→translate→rotate→restore）。②生える2経路＝なぞり中はドラッグ由来（speed>0）のみ約35%を緑化（タップは純花）／放置でも18〜34秒ごとに緑・小花がゆっくり自生するが「休息の充実度」（要素28）で止まって庭が落ち着く（無限増殖しない＝引き算を守り "最終的に出来上がる" を実現・自生は setTimeout 駆動で rAF停止中も起床し detach で確実に停止・reduce/farewell時なし）。③風で草・umbel もそよぎ（kind別係数 swayK＝umbel1.6/草・シダ1.2/花1.0）、umbel の種穂から種（緑の小さなパーティクル）が舞う。④押し花＝植物標本ボードに自動構成＝暖かい生成り（beige #E4D9C4→#DCC8AC）＋紙ノイズ地に、庭から代表を選抜（花5・緑5・小花4・つる3の上限17）して 6×4 グリッド＋余白マージンで重ならないよう再配置し、退色（彩度・明度をわずかに落とす）させ、つるを流れとして効かせ、隅に「<花名/季節> の 標本 · <漢数字日付>」＋署名 Ichi を添える。配置乱数は庭状態由来（各花の seed:kind:r）で決定的（同じ庭→同じ標本・庭が変われば構成も変わる＝一回性）。reduce時は自生・風・種なし（緑は静的に付き、押し花標本化は静止でも機能）。SA=Sonnet・レビュー=Codex（2e-1 2周＋全ブランチ2周・最終 Critical/Warning/Nit 0：自生タイマー前進順のbusy-loop・lastSwayホットパス・fern羽状の-Math.PI/2偏り真因・標本seedの時刻依存 等を修正）。設計/計画: `docs/superpowers/{specs,plans}/2026-07-08-ichi-phase2e*.md`。Phase 2f（実装済み・2026-07-08 休むと草が生える・突風で真っさら）: ①自生を**アイドル駆動**に＝Phase2eの固定18〜34秒スケジュールを改め、最後の操作（なぞり/タップ）から約3秒（IDLE_MS=3000）何もしないと、**タップしていない空いた場所**（候補5点から既存要素・直近ポインタに最も遠い点を選ぶ場所バイアス）に草・小花が芽吹き始め、以後アイドルが続く限り3〜6秒ごとに1つずつ "休息の充実度"（28）まで。操作すると `lastInteractT` が更新され次の芽吹きは操作から3秒後に後ろ倒し（操作中は生えない）。「休むほど庭が育つ」＝遅さの報酬を明示化。pointerleave（ホバーアウト）は操作扱いしない。②「真っさらにする」を**突風演出**に＝ただ消すのではなく、強い風を立てて全要素（花・草）を風向きに一斉にバースト（花3枚・緑2枚の粒子・GUST_MAX=180上限）して1.5〜2秒で庭が空になる。clear も操作としてアイドル基準を更新するので、クリア直後は3秒の静けさの後に自生が戻る。音ON時のみ強い風音（otoPlayGust＝ホワイトノイズ→lowpass700→gain0.06/2.2s・onended disconnect）。reduce時は自生・突風演出とも無効（即クリア）。テスト隔離用に自生を完全無効化する `__noSprout` フラグを追加（粒子減衰など自生と無関係な検証の隔離用）。SA=Sonnet・レビュー=Codex（全ブランチ2周・最終 Critical/Warning/Nit 0：clearのアイドル基準更新漏れ・突風時刻の集約を修正／アイドル自生が既存の待ち時間測定テストに干渉するflakyを __noSprout と上限不変条件で解消）。設計/計画: `docs/superpowers/{specs,plans}/2026-07-08-ichi-phase2f*.md`。Phase 2g（実装済み・2026-07-09 自動で仕上がる草花の一枚）: 好評だった「自動で入る自生」と「タップ(突風)取り消し」を土台に、**放置すると庭が画面いっぱいに密に自生し、暗い光の庭→明るい生成りへ移ろって、参考画像（押し花植物標本ボード "BLOOM MAISON" 系）のような美術的な一枚に勝手に仕上がる**（CEO判断: 密に敷き詰める／完成で静止／明るい生成り基調）。①自生の充実度上限 FILL_TARGET を 28→**44**（画面いっぱい）に拡大し、ambientSprout を大小のリズム（主役の大輪級 r70-100・中サイズ r40-65・小花/緑 r26-46・花が主役で緑~40%）で芽吹かせ、配置を**7×6グリッドの空きセル優先＋縁-縁クリアランス**にして全面を均等に埋める（旧「最遠点バイアス」由来の中央の暗いドーナツを構造的に解消）。②庭の充実度 **fillRatio = flowers.length / FILL_TARGET** に応じ、`opts.onFill` コールバックで mount が `.hana-stage` 背景を暗い warm-black（#0A0906）→明るい生成り beige（#E4D9C4〜#DED3BC）へ **easeOutCubic で lerp**（中間の茶濁りを圧縮）、fillRatio=1 で光だまりを不透明化して中央の暗い穴を消す。fillRatio=0（空・操作中）は従来の暗い庭と描画上完全に同一＝**操作中は暗いまま、明るくなるのは自動で仕上がった時だけ**という棲み分け。③明るい地（fillRatio>0.5）では花のグロー・接地影を弱め彩度を保って**1輪ずつ判読できる**ようにする（drawFlower 本体は不変・redraw 合成側で補正）。④FILL_TARGET 到達で自生タイマーを停止＝「静かに落ち着く」（tap/clear/redraw保険で再開）。タップ追加・突風クリア（空→暗→再び仕上がる）・reduce（自生/移ろいなし）・press（押し花標本ボードは独立して従来通り）は維持。SA=Sonnet・レビュー=Codex（全ブランチ3周・最終 Critical/Warning/Nit 0：満杯からのクリアで自生タイマーが再開しない実バグ・fr=0背景の扱い・極小canvasのNaN座標を修正／視覚磨きで全面均等・脱濁り・脱茶中間を達成）。設計/計画: `docs/superpowers/{specs,plans}/2026-07-09-ichi-phase2g*.md`。Phase 2h（実装済み・2026-07-10 密度調整・自動明るさ・図鑑連動・和の音）: CEOの6依頼。①完成密度 FILL_TARGET 44→**30**（余白を増やす）。②**大輪の重なりを減らす**＝大輪級（size>=70）は大輪同士の縁-縁クリアランスが size*0.5 未満の候補を除外し、満たす候補が無ければその周期は中サイズ以下に格下げ（団子回避）。③**背景の明るさを"自動自生で咲いた分"だけに連動**＝`ambientCount`（ambientSprout で++・clear/farewell で0・tap/なぞり/長押し大輪では増やさない）を新設し `fillRatio = ambientCount / FILL_TARGET`。**タップ/なぞりでは明るくならず、放置して自動自生が進むと明るくなる**（自生の停止判定は総数 flowers.length>=FILL_TARGET のまま）。これにより「自動が働いている」ことが背景の明るさで可視化される。④「1回操作した後に自動で咲かない気がする」件はコード精査＋回帰テストで**真のバグが無い**ことを確証（tap後 flowers≥1 で rAF 継続し redraw保険＋タイマーで最後の操作から約3秒後に自生が再開する）。体感の主因は③の明るさ信号がタップ後に変わらなかったことで、③で解消。⑤**季節と花の種類を活かす＝図鑑24種の実物を自動の庭に咲かせる**。index.html が `NOCTA_FLORA` から `flowerKinds`（各花の `accentColor`・`hanaForm`・`seasonFromFlower` で正規化した季節）を mount へ渡し、ambientSprout の花要素が `deriveSeededPalette(色, その花の季節)` ＋ `hanaForm` で咲く（桜=春・彼岸花=秋 等その花の季節の質感）。散り・風・突風の粒子色、press の押し花標本ボードも要素ごとの `f.palette` を保持し多色を維持。flowerKinds 未指定/不正はフォールバック（疎結合）。⑥**咲く音を和の五音階（陽旋法 yo scale）にクォンタイズ**＝`YO=[1,1.125,1.333,1.5,1.667]`・基音 311Hz。花のサイズで音度を選び軽いゆらぎ＋稀にオクターブ上げ。連続して咲くと自然にやさしい旋律になり、ランダムでも音階内で常に調和して**心が安らぐ**（やわらかいアタック・ノード後始末は維持・デフォルト無音）。SA=Sonnet・レビュー=Codex（全ブランチ2周・最終 Critical/Warning/Nit 0：farewellのambientCount整合・図鑑連動で各花の季節を使う・press標本の図鑑色保持・flowerKinds検証 を修正／密度30に伴う花:緑比率テストのn=30 flakyを頑健帯に是正）。設計/計画: `docs/superpowers/{specs,plans}/2026-07-10-ichi-phase2h*.md`。バックログ: 一輪挿し/絵本・bell形の磨き込み・環境音ミキサー・fernのrachisカーブ化・音の旋律の単調化の余地・季節優先度の調整。
各花からの咲かせる（実装済み・2026-07-07）: 図鑑の各花セクション24種に「この花を咲かせる」入口を追加し、その花の色(accentColor)・似た輪郭(form)・季節で固定して咲かせられる（図鑑↔遊びの橋）。手続き花形を4→7に拡張（追加: star=桔梗/水仙・layered=椿/薔薇/牡丹の八重・bell=百合/釣鐘）、24花を最適formにマッピング（flora-data の `hanaForm`）。シード付きモードでは季節チップを隠しその花で描く。汎用CTA「咲かせて遊ぶ」は自由制作（4→7形ランダム）として存置。花の大きさもランダム化。設計/計画: `docs/superpowers/{specs,plans}/2026-07-07-ichi-perflower*.md`

#### 「筆あそび」の設計要旨（Hare・2026-07-05）

- **画面構成**: 中央に和紙（紙色 = 生成り#F5EFE0 + その色6%）、周囲に「かさね配色×季節文様」の額（春=麻の葉/夏=青海波/秋=矢絣/冬=市松、SVG動的生成）
- **ペン**: 筆（丸ニブ・にじみ）とカリグラフィ（斜め45°の楕円ニブ）の2種
- **インク物理**（Canvas 2D）: 速度で太さ変化・停留でにじみ・書き続けでかすれ・払いで先細り・multiplyで紙に染みる
- **インク色**: 本色 + かさね2色 + 墨。淡色は自動で濃色版（本色60% + 墨40%）に置換してコントラスト担保
- **保存**: 紙+描線+色名クレジットを合成してPNGダウンロード（`hare-{slug}.png`）
- **アクセシビリティ**: canvasは`touch-action:none`・ダイアログの背景クリック閉じは廃止（描画と衝突するため）してESC+×のみで閉じる。フォーカストラップ・inertはcoreに一任
- **実装**: `website/iro/fude.js`（NoctaFude.mount）— iro/index.htmlは薄いラッパー

---

## 8.5. 2026-07-04 の変更（クラフト精度向上＋ステートメント帯の遊び）

| 変更 | 意図 |
|---|---|
| 和文セリフ **Shippori Mincho 600** を導入。display スタックを `'EB Garamond','Shippori Mincho',serif` に | serif-led ブランドの核である和文大見出し（ヒーロー・ステートメント）が OS 依存の明朝フォールバックだった問題を解消。Windows での見え方を統制。和文 display は letter-spacing 0.04em・line-height をやや開放（ヒーロー 1.06 / ステートメント 1.2） |
| OGP・meta description・theme-color・favicon.svg を追加 | シェア時のカード表示。og:image は暫定で SILENCE #1（IPFS）。専用バナー（1200x630）制作後に差し替えること |
| 廃止パレット残骸の掃除 | thumb-1〜6 を warm-black 面差のみのグラデーションへ（紫/シアン/ピンク廃止）。option 背景 #1a1a2e → #0F0D0A。ナビスクロール背景を warm-black 系へ。ヒーロータグライン #7a6a50 → muted トークン。フォーム状態色（#8b6914 / #00b894 / 赤グラデ）を撤去しテキスト+不透明度で表現 |
| .btn-submit を on-accent 準拠に | シルバー地×白文字（コントラスト比 約1.9:1）→ 文字色 #0A0906・pill 形状 |
| 未使用CSS/トークン削除 | .service-card / .card-top-bar / .bar-* / .brand-badge / .text-vgreen / glow-purple・cyan・pink・vgreen、i18n の services.* / news.* 全キー |
| STATS の count-up 配線 | data-count 経由に変更しスクロール到達時に駆け上がり（従来は即時代入でアニメーション未発火） |
| ステートメント帯にインタラクション追加 | ポインタ移動/タップで水彩足跡が残る（#statement-paws・既存 #cat-paw + #watercolor + 4本柱パレット流用）。「遊ぶことで彩る」の実演。最大60個・reduced-motion で無効。**他セクションへ同種の追加はしない**（希少性維持） |
| ヒーローCTA を pill に統一 | .btn-primary-cta / .btn-glass-cta に border-radius 9999px（DESIGN.md「全ボタン pill」の署名に整合） |

## 8. brandkit.html — ブランドガイド（2026-06-16 再生成）

### 役割
`brandkit.html` は **DESIGN.md から派生した、人間が一目で把握するためのブランド見本ページ**。
設計の正本（source of truth）は機械可読の `DESIGN.md` であり、Claude／コードが設計判断で参照するのも DESIGN.md。
brandkit.html は「表示物」であって「正本」ではない。**ブランド定義を変えるときは DESIGN.md を先に直す**（brandkit.html だけ直しても Claude の挙動は変わらず、両者がズレると混乱の元）。

### 使いどころ
- 外部委託先（印刷所・デザイナー・映像編集・イラストレーター）へのブリーフ。URL を渡すだけでトーンが伝わる。
- CEO 自身の視覚的リファレンス（トークン羅列より思い出しやすい）。
- 信用の担保（提携・コラボ時にブランドの一貫性を示す名刺代わり）。
- 将来、人が加わったときのオンボーディング資料。

### 公開状態
`website/` 配下なので push すると GitHub Pages の `/brandkit.html` で**誰でも閲覧可能な準公開状態**（ナビ未リンク・直URL のみ）。社内限定にしたい場合は `website/` 外へ退避する。

### 構成・規約
- 単一シルバー `#B8B4AE` × warm-black `#0A0906` × オフホワイト `#E8E0D0`。フォントは EB Garamond / Syne / Bebas Neue / Noto Sans JP。
- 旧 `brand_assets.html` を統合し削除済み（旧 gold/neon/lavender・Space Grotesk・MUSIC ENTERTAINMENT/VOCALOID は全廃）。
- セクション: Essence / Color / Typography / Logo / Four Domains / Applications / Usage。多色グラデーションのロゴは復活させない。
