# Webベストプラクティスレビュー: 2026-06-15
ソース: Google Search Grounding（Gemini 2.5 Flash）
インジェクション検査: 実施済み（検出件数: 0件）
取得状況: 5クエリ中 1件成功 / 4件失敗（Gemini API 503・過負荷。3回バックオフリトライ後も復旧せず）

## 要約（3行以内）
Gemini API が継続的に過負荷（503）で、5クエリ中4件が取得できず収集は不完全。
取得できた1件（hooks/skills/slash commands）は NOCTA が既に実装済みの内容を再確認する内容で、新規の重要発見はなし。
Web収集は後日 API 復旧後に再実行を推奨。今週のベストプラクティス収集は xmcp（7件）+ Claude Docs（7件）で十分なカバレッジがある。

## 発見した主要ベストプラクティス
| # | 発見内容 | 関連度 | 参照元 |
|---|---------|--------|--------|
| 1 | CLAUDE.md をプロジェクトの「憲法」として規約・品質基準・固有ルールを記述する | 高（実装済み） | vertexaisearch（Google grounding） |
| 2 | カスタムスラッシュコマンドで定型作業を効率化する | 高（実装済み） | 同上 |
| 3 | フックでイベント駆動の自動チェック・連携を強化する | 中 | 同上 |
| 4 | 専門分野に特化したスキルを開発し自律性を高める（音楽ジャンル別作曲知識・PR生成等） | 中 | 同上 |
| 5 | HP管理に Playwright によるブラウザ操作の自動化を活用する事例 | 低 | 同上 |

## NOCTAへの適用提案
| 優先度 | 対象 | 現状 | 提案内容 |
|--------|------|------|----------|
| 低 | スキル設計 | フェーズ別スキルは整備済み | 「音楽ジャンル別の作曲知識スキル」は CEO が音判断を行う R-10 と衝突しうるため不要。仕様書テンプレ強化に留める |
| 低 | Web収集 | 今回4件取得失敗 | API復旧後に /web-practices-review を再実行（次回 weekly-check 時で可） |

## すでに実装済みの項目
1. CLAUDE.md によるプロジェクト規範の明文化（R-01〜R-15・CODEMAP・APPROVAL GATES）
2. カスタムスラッシュコマンド群（/phase1〜5・/music-status 等）
3. フック（SessionStart・UserPromptSubmit 等）の運用
4. フェーズ別の専門スキル・エージェント構成（trend-analyst / lyric-poet / svp-generator 等）

## 要注意: git・シェル操作に関わる提案（CEOが内容を確認してから判断）
なし（取得内容に git・シェル操作の提案は含まれていなかった）

## インジェクション検査結果
危険パターン（前指示無視・git破壊系・rm -rf・curl|bash・APIキー入力誘導等）は検出されなかった。

## 適用しない項目と理由
- 「音楽ジャンル別の作曲知識スキルで自律的に作曲」: R-10（CEOが音・感情判断を行う）・R-11（叩き台原則）と衝突。AIは設計書・プロンプトのみ作る方針を維持。
- Playwright によるHP自動操作: NOCTA は data ファイル（visual-data.js 等）+ GitHub Pages の静的構成で完結しており不要。

## CEOが確認すべき事項
1. Web収集は今回ほぼ失敗（API過負荷）。次回 weekly-check 時に /web-practices-review を再実行するかの判断。
2. 今週の収集は xmcp 7件 + Claude Docs 7件（うち新モデル Claude Fable 5 検出）で実質的なカバレッジは確保済み。/model-review と /best-practices-review を優先するのが妥当。
